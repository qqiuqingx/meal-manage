# 排餐计划接口文档

## 基础信息

- **基础路径**: `/api/meal-plan`
- **认证方式**: JWT Token (Bearer Token)
- **权限前缀**: `mealPlan:`

## 枚举值说明

### 餐次类型 (meal_type)

| code | desc |
|------|------|
| BREAKFAST | 早餐 |
| LUNCH | 午餐 |
| DINNER | 晚餐 |
| LUNCH_DINNER | 午晚餐共享餐数池（仅预警接口返回） |

### 计划状态 (status)

| code | desc |
|------|------|
| GENERATING | 生成中 |
| SUCCESS | 成功 |
| FAILED | 失败（部分成功或全部失败） |

### 客户计划状态 (status)

| code | desc |
|------|------|
| 0 | 失败 |
| 1 | 成功 |

### 菜品类型 (dish_type)

| code | desc |
|------|------|
| MAIN | 主菜 |
| SIDE | 副菜 |
| VEGETABLE | 素菜 |
| SOUP | 汤 |
| RICE | 米饭 |

### 替换原因 (replace_reason)

| code | desc |
|------|------|
| ALLERGY | 过敏回退 |
| NEXT_DAY | 次日回退 |
| 客户选择替换 | 客户指定米饭类型触发的替换标记 |
| ORDER_RULE | 订单级换菜规则（customer_order_replace_rule 配置的自动替换） |

### 过敏相关字段

| 字段 | 类型 | 说明 |
|------|------|------|
| isAllergyFiltered | Boolean | 是否因过敏被过滤 |
| allergyReasons | String | 触发的过敏标签，多个用逗号分隔（如 "海鲜,豆类"），仅在 isAllergyFiltered=true 时有值 |

---

## 接口列表

### 1. 生成排餐计划

| 项目 | 说明 |
|------|------|
| **接口地址** | `POST /api/meal-plan/generate` |
| **权限** | `mealPlan:generate` |
| **Content-Type** | `application/json` |

**请求体 (JSON)**:

| 参数名 | 类型 | 必填 | 说明 | 示例 |
|--------|------|------|------|------|
| recordDate | String | 是 | 排餐日期，格式 yyyy-MM-dd | 2026-04-02 |
| mealType | String | 是 | 餐次，支持 LUNCH / DINNER | LUNCH |

**请求示例**:
```json
{
  "recordDate": "2026-04-02",
  "mealType": "LUNCH"
}
```

**响应示例**:
```json
{
  "mealPlanId": 1,
  "recordDate": "2026-04-02",
  "mealType": "LUNCH",
  "totalCount": 100,
  "successCount": 98,
  "failCount": 2,
  "failDetails": [
    {
      "failReason": "荤菜不足"
    },
    {
      "failReason": "客户档案不存在"
    }
  ]
}
```

---

### 2. 查询排餐计划列表

| 项目 | 说明 |
|------|------|
| **接口地址** | `GET /api/meal-plan` |
| **权限** | `mealPlan:list` |

**请求参数 (Query)**:

| 参数名 | 类型 | 必填 | 说明 | 示例 |
|--------|------|------|------|------|
| recordDate | String | 否 | 排餐日期 | 2026-04-02 |
| mealType | String | 否 | 餐次 | LUNCH |
| status | String | 否 | 状态 | SUCCESS |
| page | Integer | 否 | 页码，默认 0 | 0 |
| size | Integer | 否 | 每页数量，默认 10 | 10 |

**响应示例**:
```json
{
  "content": [
    {
      "id": 1,
      "recordDate": "2026-04-02",
      "mealType": "LUNCH",
      "totalCount": 100,
      "successCount": 98,
      "failCount": 2,
      "status": "FAILED",
      "generateTime": "2026-04-02 10:30:00",
      "deleted": false
    }
  ],
  "totalElements": 1
}
```

---

### 3. 查询排餐计划详情

| 项目 | 说明 |
|------|------|
| **接口地址** | `GET /api/meal-plan/{id}` |
| **权限** | `mealPlan:list` |

**路径参数**:

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| id | Long | 是 | 排餐计划ID |

**响应示例**:
```json
{
  "id": 1,
  "recordDate": "2026-04-02",
  "mealType": "LUNCH",
  "totalCount": 100,
  "successCount": 98,
  "failCount": 2,
  "status": "FAILED",
  "generateTime": "2026-04-02 10:30:00",
  "deleted": false
}
```

---

### 4. 查询排餐计划完整详情（含客户列表和菜品明细）

一次返回所有数据，无需多次调用接口。

| 项目 | 说明 |
|------|------|
| **接口地址** | `GET /api/meal-plan/{mealPlanId}/detail` |
| **权限** | `mealPlan:list` |

**路径参数**:

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| mealPlanId | Long | 是 | 排餐计划ID |

**响应示例**:
```json
{
  "mealPlan": {
    "id": 1,
    "recordDate": "2026-04-02",
    "mealType": "LUNCH",
    "totalCount": 100,
    "status": "SUCCESS",
    "generateTime": "2026-04-02 10:30:00"
  },
  "customers": [
    {
      "id": 1,
      "customerId": 100,
      "customerName": "张三",
      "customerCode": "A001",
      "phone": "13800138000",
      "orderId": 200,
      "firstMealOfOrder": true,
      "parentPackageId": 10,
      "childPackageId": 20,
      "status": 1,
      "failReason": null,
      "meatRequiredCount": 2,
      "vegRequiredCount": 1,
      "includeSoup": 1,
      "includeRice": 1,
      "specialRequirements": "少盐少油",
      "items": [
        {
          "id": 1,
          "dishType": "MAIN",
          "dishId": 10,
          "dishName": "红烧肉",
          "seq": 1,
          "isReplaced": false,
          "originalDishId": null,
          "originalDishName": null,
          "replaceReason": null,
          "isAllergyFiltered": false,
          "allergyReasons": null,
          "ingredients": [
            {
              "ingredientId": 5,
              "ingredientName": "五花肉",
              "unit": "克",
              "quantity": 300.0,
              "remark": null
            },
            {
              "ingredientId": 8,
              "ingredientName": "生抽",
              "unit": "毫升",
              "quantity": 15.0,
              "remark": null
            }
          ]
        },
        {
          "id": 2,
          "dishType": "VEGETABLE",
          "dishId": 30,
          "dishName": "清炒时蔬",
          "seq": 2,
          "isReplaced": true,
          "originalDishId": 35,
          "originalDishName": "麻婆豆腐",
          "originalDishType": null,
          "replaceReason": "ALLERGY",
          "isAllergyFiltered": true,
          "allergyReasons": "豆类",
          "ingredients": [
            {
              "ingredientId": 12,
              "ingredientName": "青菜",
              "unit": "克",
              "quantity": 200.0,
              "remark": null
            },
            {
              "ingredientId": 15,
              "ingredientName": "蒜",
              "unit": "克",
              "quantity": 5.0,
              "remark": null
            }
          ]
        },
        {
          "id": 3,
          "dishType": "MAIN",
          "dishId": 40,
          "dishName": "清蒸鱼",
          "seq": 3,
          "isReplaced": false,
          "originalDishId": null,
          "originalDishName": null,
          "replaceReason": null,
          "isAllergyFiltered": false,
          "allergyReasons": null,
          "ingredients": []
        },
        {
          "id": 4,
          "dishType": "MAIN",
          "dishId": 50,
          "dishName": "红烧排骨",
          "seq": 4,
          "isReplaced": true,
          "originalDishId": 10,
          "originalDishName": "红烧肉",
          "originalDishType": "MAIN",
          "replaceReason": "ORDER_RULE",
          "isAllergyFiltered": false,
          "allergyReasons": null,
          "ingredients": []
        }
      ]
    }
  ],
  "totalCustomers": 100,
  "successCount": 98,
  "failCount": 2
}
```

补充说明：
- 当 `dishType=RICE` 且订单 `riceType != 默认` 时，接口会返回 `isReplaced=true`、`replaceReason=客户选择替换`。
- 此时 `originalDishId/originalDishName` 表示当天菜单基准米饭候选（排序后的首个米饭候选），不是最终实际选中的回退米饭。
- 当 `replaceReason=ORDER_RULE` 时，表示该菜品由订单级换菜规则自动替换，`originalDishType` 为被替换前的菜品类型（跨类型替换时有值）。
- 订单级换菜规则优先级最高，被替换的菜品跳过过敏/忌口过滤。

---

### 5. 删除排餐计划

根据日期和餐次删除排餐计划（级联删除客户和明细）。

| 项目 | 说明 |
|------|------|
| **接口地址** | `DELETE /api/meal-plan` |
| **权限** | `mealPlan:del` |

**请求参数 (Query)**:

| 参数名 | 类型 | 必填 | 说明 | 示例 |
|--------|------|------|------|------|
| recordDate | String | 是 | 排餐日期，格式 yyyy-MM-dd | 2026-04-02 |
| mealType | String | 是 | 餐次（LUNCH/DINNER） | LUNCH |

**响应**: `204 No Content`

**示例**:
```
DELETE /api/meal-plan?recordDate=2026-04-02&mealType=LUNCH
```

---

### 6. 批量删除排餐计划

根据ID列表批量删除排餐计划（级联删除客户和明细）。

| 项目 | 说明 |
|------|------|
| **接口地址** | `DELETE /api/meal-plan/batch` |
| **权限** | `mealPlan:del` |
| **Content-Type** | `application/json` |

**请求体 (JSON)**:

```json
[1, 2, 3]
```

**响应**: `204 No Content`

---

### 7. 批量删除客户排餐计划

根据客户计划ID列表批量删除客户排餐计划（级联删除明细）。

| 项目 | 说明 |
|------|------|
| **接口地址** | `DELETE /api/meal-plan/customers` |
| **权限** | `mealPlan:del` |
| **Content-Type** | `application/json` |

**请求体 (JSON)**:

```json
[1, 2, 3, 4, 5]
```

**响应**: `204 No Content`

---

### 8. 查询排餐计划客户配送地址

根据排餐计划ID查询其中所有客户的手机号、客户编号和配送地址。

**地址选择规则**（根据排餐计划日期自动选择）：
- 周末（周六/周日）→ `WEEKEND` 地址
- 工作日（周一至周五）→ `WORKDAY` 地址
- 指定类型地址不存在时，fallback 到 `DEFAULT` 地址
- 都没有则返回 null

| 项目 | 说明 |
|------|------|
| **接口地址** | `GET /api/meal-plan/{mealPlanId}/customer-addresses` |
| **权限** | `mealPlan:list` |

**路径参数**:

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| mealPlanId | Long | 是 | 排餐计划ID |

**响应示例**:
```json
[
  {
    "customerId": 100,
    "customerCode": "A001",
    "phone": "13800138000",
    "addressDetail": "朝阳区XX路XX号",
    "addressType": "WORKDAY",
    "allergyTags": ["海鲜", "豆类"],
    "specialRequirements": "少盐少油"
  },
  {
    "customerId": 101,
    "customerCode": "A002",
    "phone": "13900139000",
    "addressDetail": "海淀区XX大厦XX室",
    "addressType": "DEFAULT",
    "allergyTags": [],
    "specialRequirements": null
  }
]
```

**响应字段说明**:

| 字段 | 类型 | 说明 |
|------|------|------|
| customerId | Long | 客户ID |
| customerCode | String | 客户编号 |
| phone | String | 手机号 |
| addressDetail | String | 配送地址（根据日期选定） |
| addressType | String | 实际使用的地址类型：`DEFAULT` / `WORKDAY` / `WEEKEND`，无地址时为 null |
| allergyTags | List\<String\> | 过敏食物标签列表，无过敏时为空数组 |
| specialRequirements | String | 特殊要求，无特殊要求时为 null |

---

### 9. 查询餐数耗尽预警

查询指定日期排餐后即将耗尽餐数的客户订单。接口按餐数池分别计算：
- 早餐池：`breakfast_count - BREAKFAST 已核销数`
- 午晚餐池：`lunch_dinner_count - LUNCH 已核销数 - DINNER 已核销数`

当某个餐数池当前剩余餐数为 1，或目标日期该餐数池排餐数会使剩余餐数耗尽时，返回预警记录。

| 项目 | 说明 |
|------|------|
| **接口地址** | `GET /api/meal-plan/depletion-warnings` |
| **权限** | `mealPlan:list` |

**请求参数 (Query)**:

| 参数名 | 类型 | 必填 | 说明 | 示例 |
|--------|------|------|------|------|
| date | String | 否 | 目标日期，格式 yyyy-MM-dd；不传默认明天 | 2026-05-29 |

**响应示例**:
```json
[
  {
    "orderId": 200,
    "customerId": 100,
    "customerCode": "A001",
    "customerName": "张三",
    "mealType": "LUNCH_DINNER",
    "mealTypeName": "午晚餐",
    "remainingCount": 2,
    "tomorrowScheduledCount": 2
  }
]
```

**响应字段说明**:

| 字段 | 类型 | 说明 |
|------|------|------|
| orderId | Long | 订单ID |
| customerId | Long | 客户ID |
| customerCode | String | 客户编号 |
| customerName | String | 客户姓名 |
| mealType | String | 预警餐数池：`BREAKFAST` / `LUNCH_DINNER` |
| mealTypeName | String | 预警餐数池名称：早餐 / 午晚餐 |
| remainingCount | Integer | 当前餐数池剩余餐数 |
| tomorrowScheduledCount | Integer | 目标日期该餐数池排餐数 |

---

## 权限配置

需要在权限管理中配置以下权限点：

| 权限代码 | 权限名称 | 说明 |
|----------|----------|------|
| `mealPlan:list` | 排餐计划列表 | 查看排餐计划列表和详情 |
| `mealPlan:generate` | 生成排餐计划 | 生成排餐计划 |
| `mealPlan:del` | 删除排餐计划 | 删除排餐计划 |

---

## 更新日志

- 2026-04-02: 初始版本
- 2026-04-02: 新增聚合查询接口 `GET /api/meal-plan/{mealPlanId}/detail`，一次返回完整排餐计划数据（含客户列表和菜品明细）
- 2026-04-02: 文档中移除客户列表、客户详情、菜品明细查询接口（代码保留，如有需要可启用）
- 2026-04-02: `GET /api/meal-plan/{mealPlanId}/detail` 接口响应中新增菜品配料列表（ingredients 字段）
- 2026-04-12: `GET /api/meal-plan/{mealPlanId}/detail` 接口响应中新增过敏过滤字段 `isAllergyFiltered` 和 `allergyReasons`（菜品明细 items 数组），用于标识因客户过敏被过滤的菜品
- 2026-04-15: `GET /api/meal-plan/{mealPlanId}/customer-addresses` 接口响应中新增 `allergyTags` 和 `specialRequirements` 字段，返回客户过敏信息和特殊要求
- 2026-04-13: 新增 `GET /api/meal-plan/{mealPlanId}/customer-addresses` 接口，查询排餐计划中客户的手机号、客户编号和配送地址（根据排餐日期自动选择工作日/周末/默认地址）
- 2026-04-19: `GET /api/meal-plan/{mealPlanId}/detail` 接口响应中新增 `specialRequirements` 字段（客户排餐列表 customers 数组），返回客户特殊要求
- 2026-05-06: 新增 `ORDER_RULE` 替换原因，菜品明细 items 新增 `originalDishType` 字段，支持订单级换菜规则自动替换
- 2026-05-06: `GET /api/meal-plan/{mealPlanId}/detail` 接口响应中新增 `firstMealOfOrder` 字段（customers 数组），标识订单+餐次维度的首次成功排餐
- 2026-05-28: 新增 `GET /api/meal-plan/depletion-warnings` 接口，按早餐、午晚餐餐数池返回餐数耗尽预警
