# 核销管理接口文档

## 基础信息

- **基础路径**: `/api/meal-verification`
- **认证方式**: JWT Token (Bearer Token)
- **权限前缀**: `mealPlan:`

---

## 接口列表

### 1. 执行核销

| 项目 | 说明 |
|------|------|
| **接口地址** | `POST /api/meal-verification/verify` |
| **权限** | `mealPlan:list` |
| **Content-Type** | `application/json` |

**请求体 (JSON)**:

| 参数名 | 类型 | 必填 | 说明 | 示例 |
|--------|------|------|------|------|
| customerPlanIds | Array[Long] | 是 | 客户排餐ID列表 | [1, 2, 3] |
| remark | String | 否 | 备注 | 批量核销 |

**请求示例**:
```json
{
  "customerPlanIds": [1, 2, 3],
  "remark": "批量核销"
}
```

**响应示例**:
```json
{
  "successCount": 2,
  "failCount": 1,
  "failReasons": [
    "客户ID 3: 订单剩余餐数不足，当前剩余: 0"
  ]
}
```

---

### 2. 根据ID查询核销日志

| 项目 | 说明 |
|------|------|
| **接口地址** | `GET /api/meal-verification/logs/{id}` |
| **权限** | `mealPlan:list` |

**路径参数**:

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| id | Long | 是 | 核销日志ID |

**响应示例**:
```json
{
  "id": 1,
  "mealPlanCustomerId": 100,
  "mealPlanId": 10,
  "customerId": 1000,
  "orderId": 200,
  "recordDate": "2026-04-05",
  "mealType": "LUNCH",
  "verificationCount": 1,
  "remainingBefore": 10,
  "remainingAfter": 9,
  "verifiedTotalBefore": 5,
  "verifiedTotalAfter": 6,
  "operator": "admin",
  "operateTime": "2026-04-05 14:30:00",
  "remark": "常规核销",
  "createTime": "2026-04-05 14:30:00"
}
```

---

### 3. 根据订单ID查询核销日志列表

| 项目 | 说明 |
|------|------|
| **接口地址** | `GET /api/meal-verification/logs/order/{orderId}` |
| **权限** | `mealPlan:list` |

**路径参数**:

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| orderId | Long | 是 | 订单ID |

**响应示例**:
```json
[
  {
    "id": 1,
    "mealPlanCustomerId": 100,
    "orderId": 200,
    "recordDate": "2026-04-05",
    "mealType": "LUNCH",
    "verificationCount": 1,
    "remainingBefore": 10,
    "remainingAfter": 9,
    "verifiedTotalBefore": 5,
    "verifiedTotalAfter": 6,
    "operator": "admin",
    "operateTime": "2026-04-05 14:30:00",
    "remark": "常规核销"
  },
  {
    "id": 2,
    "mealPlanCustomerId": 101,
    "orderId": 200,
    "recordDate": "2026-04-06",
    "mealType": "DINNER",
    "verificationCount": 1,
    "remainingBefore": 9,
    "remainingAfter": 8,
    "verifiedTotalBefore": 6,
    "verifiedTotalAfter": 7,
    "operator": "admin",
    "operateTime": "2026-04-06 18:00:00",
    "remark": "常规核销"
  }
]
```

### 4. 分页查询核销记录

| 项目 | 说明 |
|------|------|
| **接口地址** | `GET /api/meal-verification/logs` |
| **权限** | `mealPlan:list` |

**请求参数（Query String）**:

| 参数名 | 类型 | 必填 | 说明 | 示例 |
|--------|------|------|------|------|
| page | Integer | 否 | 页码，从 0 开始，默认 0 | 0 |
| size | Integer | 否 | 每页数量，默认 10 | 10 |
| customerName | String | 否 | 客户名称（模糊匹配） | 张三 |
| orderId | Long | 否 | 订单ID（精确匹配） | 200 |
| mealType | String | 否 | 餐次（LUNCH/DINNER） | LUNCH |
| recordDateStart | String | 否 | 排餐日期开始（yyyy-MM-dd） | 2026-04-01 |
| recordDateEnd | String | 否 | 排餐日期结束（yyyy-MM-dd） | 2026-04-30 |
| operateTimeStart | String | 否 | 操作时间开始（yyyy-MM-dd） | 2026-04-01 |
| operateTimeEnd | String | 否 | 操作时间结束（yyyy-MM-dd） | 2026-04-30 |
| operator | String | 否 | 操作人（精确匹配） | admin |

**响应示例**:
```json
{
  "content": [
    {
      "id": 1,
      "mealPlanCustomerId": 100,
      "mealPlanId": 10,
      "customerId": 1000,
      "orderId": 200,
      "recordDate": "2026-04-05",
      "mealType": "LUNCH",
      "verificationCount": 1,
      "remainingBefore": 10,
      "remainingAfter": 9,
      "verifiedTotalBefore": 5,
      "verifiedTotalAfter": 6,
      "operator": "admin",
      "operateTime": "2026-04-05T14:30:00.000+0800",
      "remark": "常规核销",
      "createTime": "2026-04-05T14:30:00.000+0800",
      "customerName": "张三",
      "customerCode": "A001",
      "parentPackageName": "月子餐"
    }
  ],
  "totalElements": 50
}
```

---

## 业务说明

### 核销流程

1. **校验客户排餐记录是否存在**
2. **检查是否已核销**（已核销则不允许重复核销）
3. **检查订单剩余餐数是否足够**（剩余餐数 > 0）
4. **计算核销单价**：
   - 午餐/晚餐：使用订单的 `lunch_dinner_price`
   - 早餐：使用订单的 `breakfast_price`
5. **原子性更新订单**：
   - `verified_count` + 1（已核销餐数）
   - `verified_amount` + 单价（核销金额）
   - `meal_balance` + 单价（餐费余额）
   - `remaining_count` - 1（剩余餐数）
6. **更新客户排餐核销状态**：
   - `is_verified` = 1
   - `verification_time` = 当前时间
   - `verification_operator` = 当前操作人
7. **记录核销日志**到 `meal_verification_log` 表

### 核销失败原因

| 错误信息 | 说明 |
|----------|------|
| 客户排餐记录不存在 | 客户排餐ID无效 |
| 该客户已完成核销，请勿重复操作 | 该客户已被核销过 |
| 关联订单不存在 | 订单数据异常 |
| 订单剩余餐数不足 | 订单没有剩余餐数可核销 |
| 订单单价配置异常，无法核销 | 订单的 lunchDinnerPrice 或 breakfastPrice 为空或≤0 |
| 订单更新失败 | 并发冲突或其他数据库错误 |

### 数据库字段说明

#### meal_verification_log（核销日志表）

| 字段 | 类型 | 说明 |
|------|------|------|
| id | BIGINT | 主键 |
| meal_plan_customer_id | BIGINT | 客户排餐ID |
| meal_plan_id | BIGINT | 排餐计划ID |
| customer_id | BIGINT | 客户ID |
| order_id | BIGINT | 关联订单ID |
| record_date | DATE | 排餐日期 |
| meal_type | VARCHAR(20) | 餐次（LUNCH/DINNER） |
| verification_count | INT | 核销餐数（默认1） |
| remaining_before | INT | 核销前剩余餐数 |
| remaining_after | INT | 核销后剩余餐数 |
| verified_total_before | INT | 核销前已核销总数 |
| verified_total_after | INT | 核销后已核销总数 |
| operator | VARCHAR(50) | 操作人 |
| operate_time | DATETIME | 操作时间 |
| remark | VARCHAR(255) | 备注 |

#### meal_plan_customer（排餐客户表）新增字段

| 字段 | 类型 | 说明 |
|------|------|------|
| is_verified | TINYINT(1) | 是否已核销（0未核销，1已核销） |
| verification_time | DATETIME | 核销时间 |
| verification_operator | VARCHAR(50) | 核销操作人 |

---

## 更新日志

- 2026-04-08: 新增分页查询核销记录接口（接口4）
- 2026-04-06: 新增核销金额和餐费余额联动功能
- 2026-04-05: 初始版本
