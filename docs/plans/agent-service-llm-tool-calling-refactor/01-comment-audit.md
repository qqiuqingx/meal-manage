# 阶段 1：注释审查

## 检查项

- 检查 Agent Service 新增和修改的 Java 方法是否说明用途、关键参数和返回值。
- 检查主系统统一查询、权限、会话、审计及 HTTP 适配方法是否有方法注释。
- 检查删除旧链路后的接口文档、业务文档和工具契约是否没有误导性旧注释。
- 检查候选菜客户—订单关系校验等近期收尾改动是否同步说明。

## 完成标准

- 新增或修改的公共方法具备 Javadoc；私有辅助方法具备用途注释或明确命名和邻近说明。
- `git diff --check` 无错误。
- 注释不改变运行时行为，不引入旧架构名称作为当前契约。
