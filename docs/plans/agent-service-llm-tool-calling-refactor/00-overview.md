# Agent Tool Calling 重构收尾计划

## 目标

审查本次 Agent Tool Calling 重构涉及的新增、修改和删除代码，确保新增或修改的方法都有清晰注释；完成验证后，只提交本次重构相关文件。

## 阶段

1. `01-comment-audit.md`：按 Java、主系统、前端和文档范围审查注释及工作区边界，补齐缺失注释。
2. `02-verify-and-commit.md`：执行差异检查与必要测试，按范围暂存并创建中文 Git 提交。

## 范围约束

- 保留工作区中与本次重构无关的既有修改，不纳入提交。
- 不恢复已删除的旧 Planner、Capability 或直连诊断链路。
- 不修改业务逻辑；本次收尾只补注释、计划记录和提交元数据。
