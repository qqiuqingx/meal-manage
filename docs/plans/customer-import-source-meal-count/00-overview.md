# 批量导入原表餐数与历史核销修正

## 背景与目标

原表 `26年9月` 第 412 行 F1613：`I412=7`、`J412=0`、9 月 26—27 日未来日格 4 份。现导入将午晚/合计写为 4、已核销 0、剩余 4。用户确认应为午晚/合计 7、导入前已核销 3、剩余 4；未来 4 份继续排餐。早餐不导入。

## 影响范围与公共契约

- 导入午晚餐购买数取工作簿午晚餐明细的 I 列合计。导入后可用数仍为 J 列 + 导入日期之后午晚餐日格数量。
- 历史已核销基数 = 购买数 − 导入后可用数。订单 `verified_count` 以此为初值；新增 `imported_verified_count` 记录来源基数，不伪造逐餐核销日志。普通订单该字段默认为 0。
- 导入订单开始日期为导入日期的下一天，与“导入日期及之前按历史处理、之后属于未来”的日格边界一致。
- 订单剩余餐数仍满足 `早餐数 + 午晚餐数 − 已核销数`；后续真实核销继续正常递减。餐次池与日历统计应加上导入历史基数。
- 当前工作簿 `/Users/qqx/job/code/erp/2025客户用餐计划表3.xlsx` 只读分析；其文件哈希与旧计划快照不同，不能据此无条件批量回填现有订单。

## 规则来源与实施约束

- 已读取 `/Users/qqx/.codex/AGENTS.md`、仓库根 `AGENTS.md`，目标目录无更具体规则；用户对原表计数与未来排餐的最新说明优先于旧导入计划。
- 保留无关未提交改动，不执行数据库变更或客户数据修复。提供增量 SQL 与明确的存量修复路径。
- Java 8、Spring Boot 2.7、MyBatis-Plus；复用现有实体和服务，不新增依赖。修改方法写清参数和返回，实体字段有业务注释。
- 生产代码不加测试专用逻辑；测试只用匿名样例，不复制真实工作簿内容或个人信息入库/仓库。
- 后端按影响运行相关编译/测试；文档同步更新。数据库读写遵守项目数据安全规则。

## 阶段

| 阶段 | 目标 | 状态 |
| --- | --- | --- |
| 01 | 来源餐数、历史基数与订单写入 | complete |
| 02 | 排餐、统计与编辑订单口径 | complete |
| 03 | 文档、回归验证与存量数据修复方案 | complete |

## 风险与回滚

新字段需在部署代码前执行增量 SQL。程序回滚可保留可空/默认 0 字段；既有订单更正需按明确订单 ID、来源文件和导入日期生成预览后单独执行，不能以当前已变化的工作簿全量覆盖。
