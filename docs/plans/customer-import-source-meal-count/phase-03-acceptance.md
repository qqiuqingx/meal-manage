# Phase 03 文档、验证与存量修复

## 执行前规则检查

核对当前规则链、接口文档和前两阶段交付记录。

## 目标

完成接口/业务文档、定向验证，提供现有导入订单的安全修复方式。

## 前置依赖

依赖 Phase 01/02 的字段和排餐契约；需读取两阶段交付记录。

## 本阶段实施约束

不使用当前变化后的工作簿无条件覆盖数据库；存量修复按客户编号和唯一订单 ID 审核，不执行全表更新或删除。

## 涉及文件与步骤

- 更新客户导入、订单、排餐和核销文档的购买数、历史基数、剩余数关系。
- 运行相关 Maven 测试、文档/差异检查；记录已知无关测试失败。
- 核对 F1613 当前系统订单与上传来源哈希，准备限定订单的修复工具；执行数据库更新前须有可复核目标与备份。

## 验证方式与完成标准

F1613 预览为 7/3/4，未来日期仍存在；存量修复策略可审计，报告迁移和实际数据处理状态。

## 状态

complete

## 阶段交付记录

- 当前工作簿只读核对：F1613 的来源 I=7、J=0；9 月 26—27 日未来午晚餐 4 份，历史基数 3。数据库只读核对其唯一订单为 `ORD20260925320`（订单 ID 393），当前为午晚 4、核销 0、剩余 4、开始日期 9 月 25 日。
- `scripts/repair_imported_order_meal_counts.py` 默认只读；每笔更新均核对旧餐数、未来日期、核销日志和排餐，补建订单还核对客户手机号、唯一父套餐编号池与无现存订单。`--apply` 要求工作簿 SHA、精确更新/补建数量及专用备份文件，写入在单事务中完成。
- 当前工作簿 SHA-256 为 `2e18d3d82dde3cd4132a838b656cd7e817104cd0f8d0b0e3658c89d907904a59`。同批次只读审核：320 笔订单中 311 笔可更正，8 笔无需更正，1 笔来源餐数小于来源剩余数与未来日格之和，已排除；另外 38 位已建档客户缺少历史首单，均可补建。两位客户的含汤描述不一致，按现有导入器规则采用首个取值。
- 定向 Maven 测试 37 项通过；订单页修改文件 ESLint、`git diff --check`、修复脚本离线候选校验通过。扩展运行订单/排餐相关测试类时，62 项中另有 19 项失败和 10 项错误，集中在既有排餐夹具与缺失登录上下文；本次新增的定向场景均通过，未将扩展测试报告为全通过。

### 实际执行记录（2026-09-25）

- 用户确认修复 311 笔现有订单、补建 38 笔历史订单，并排除来源公式冲突的 A129。执行前保存 320 笔旧订单、38 位缺单客户及旧表结构的私有快照 `/private/tmp/customer-import-count-before-repair-20260925.json`。
- 已执行 `20260925_customer_order_imported_verified_count.sql` 并验证字段存在。用户通过 IntelliJ 重启本地后端，重启后的进程监听 8000。重跑只读审核仍为 311/38。
- 修复脚本以预期工作簿 SHA、精确更新/补建数量和排除项执行；事务提交结果 `committed_updates=311 committed_inserts=38`。脚本另存私有回滚数据 `/private/tmp/customer-import-count-repair-apply-backup-20260925.json`。
- 只读复核：F1613 唯一订单 ID 393 为午晚 7、导入历史核销 3、已核销 3、剩余 4、开始日期 2026-09-26；9 月 26—27 日四个未来日格合计仍为 4。订单页面也显示 7/3/4。
- 批次订单总数 358，其中 349 笔历史基数大于 0；`早餐 + 午晚 - 已核销 = 剩余` 无异常，导入批次内无重复客户订单。A129 的 34/0/34 保持原值，留待原表公式单独核对。

## 部署与修复顺序

1. 备份当前数据库；执行 `eladmin/sql/20260925_customer_order_imported_verified_count.sql`。
2. 部署本次后端与前端代码，再用相同工作簿、导入日期及订单编号前缀重跑修复脚本默认只读审核。
3. 人工核对私有审核清单、工作簿 SHA、311 笔更新、38 笔补建与那 1 笔来源冲突的排除项。
4. 确认后运行 `scripts/repair_imported_order_meal_counts.py`，传入同一 `--workbook`、`--import-date 2026-09-25`、`--order-code-prefix ORD20260925`、`--env-file eladmin/eladmin-system/.env.local`，并带上 `--apply --expected-count 311 --expected-insert-count 38 --expected-sha256 2e18d3d82dde3cd4132a838b656cd7e817104cd0f8d0b0e3658c89d907904a59 --backup-file <私有备份路径> --all --include-missing-orders --exclude-customer-code A129`。实际数量若变化，脚本会拒绝提交。
5. 验证 F1613 变为午晚/合计 7、已核销 3、剩余 4，且 9 月 26—27 日未来计划保持 4 份；来源冲突记录单独业务核对。
